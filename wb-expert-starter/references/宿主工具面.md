# 角色工具面（本宿主：WorkBuddy / CodeBuddy）

> **本文件是生成的**，由 `roles/tool-face.json`〔仓外〕产出（生成器 `roles/build-tool-face.mjs`〔仓外〕）。
> 不要手改：改表 → 重跑生成器。装配器每次装配都会核对，不一致即装配失败——手改会在下一次装配时被抹掉，而不是悄悄生效。
> 生成时点（表内 updated 字段）：2026-09-20。

## 一、这份文件在回答什么

同一个编辑部有两个宿主形态：DSH（原生预设）与 WorkBuddy（本专家包）。两边人格相同、机制相同，但**宿主能执行什么约束完全不同**。
这份文件把差异写清楚，只为一件事：**不让读者以为本形态拥有它没有的强制力**。

- 「能力」是宿主无关的（写账本 / 读文件 / 跑命令 / 派生子代理 / 把没加载的工具翻出来 …）。
- 「工具真名」是宿主相关的：同一个能力在两个宿主下名字不同，**且一方的真名拿到另一方可能根本不存在**。
- 本宿主还要再按**形态**分一次：主编座位 / 团队成员 / 普通子代理三者的工具面实测不同，同一份名单在三种形态下不是同一件事。

## 二、本形态的强制力现状（先说结论）

**机制已实测 · 载体未落地**（落点在项目级定义，本包尚未投放）。WorkBuddy/CodeBuddy 形态：**机制已实测、载体未落地**。① **落点不在插件包内**——宿主组件装载器**不枚举专家包**（2026-09-22 宿主日志：11 个插件各有 `Loaded plugin components for X: N agent(s)`，本包一次都没出现；同日志 `[AgentManager] volatile plugin agent override updated: chief-editor (source=novel-forge-editorial@my-experts)` 说明专家包只走「**单槽绑定 lead**」这条路）⇒ 包内 `agents/` 那两份**不是注册载体**（0.1.7 补的前言块与 `agents` 登记，对这条装载器不生效——它们的真实用途是**人格文本的权威来源**）。② **真正的落点是项目级自定义 agent 定义**：`<工作区>/.codebuddy/agents/*.md`；用户级＝app 进程环境变量 `CODEBUDDY_CONFIG_DIR` 下的 `agents/`（**本机该变量指向宿主自己的配置目录 `.workbuddy`**）；⇒ **用户级实为「宿主配置目录/agents/」**，本机是 `~/.workbuddy/agents/`——**不要照抄 `~/.codebuddy/agents/`**（那是我 09-22 早先写错的一处，已更正）；**换台机器以环境变量为准，不要以本条的字面值为准**。③ **实测三件（WB 侧 `[硬返回]`）**：能派 ✓（`Task(subagent_type="author")` 一次成功）、**人格注入** ✓（自报并准确引用 `AUTH-01…19` 条款号）、**deny 拦得住** ✓（中性同形探针 `disallowedTools:[mcp__novelist__novel_chapter]` → `Error: Permission to use mcp__novelist__novel_chapter has been denied.`，拦在权限层、未到工具本体）。④ **分层如实记**：deny 读数来自**同形配置的中性座位**；**主笔本座无机械读数**（两次自守拒发，读数 `REFUSED-BY-SELF`）⇒ 对外口径只能是「机制已实测成立（同形配置）」，**不许写成「主笔已被机器封死」**。⑤ **热加载**：插件 agent 定义随组件加载（改了要重启）；**项目级自定义定义当次生效**（实测不需重启）——两条不同的路，别混。⑥ **本包当前生效的 deny ＝ 0 条**（还没有任何一份定义投放到项目级落点）。判据：WB 侧 09-22 实跑 ＋ 宿主日志；**仓侧未独立复现该读数**（本机无法驱动该宿主的 agent 回合），但三处宿主源码事实（`getProjectAgentsDir` 写死 `.codebuddy`／`parseAgentFile` 读 `disallowedTools`／`AgentTask` 把 `agentConfig.disallowedTools` 并进子会话）已由仓侧**独立读源码坐实**。

对比 DSH 形态：那边的角色工具面是**宿主强制**的——子代理的 deny 名单由宿主在挂载期编译，写错名字直接挂载失败（不是失败在第一次派工），
而且过滤作用于子代理继承到的整个工具面。本形态目前只有**声明**：下面第三节的名单是"应该封掉什么"，不是"已经封掉什么"。

三种形态（映射的键，不是角色的属性）：

- **seat**：主编座位（专家组长本尊）——工具最全
- **teammate**：团队成员（本专家只有主笔是常驻成员）——实测缺 Edit/TaskStop 等、且缺 Agent/TeamCreate
- **subagent**：普通子代理（五个按需工种以派工提示词交给它跑）——实测缺 Agent/TeamCreate，但有 ToolSearch/DeferExecuteTool

## 三、角色 × 能力（本宿主工具真名）

| 角色 | 形态 | 封掉的能力 | 本宿主工具真名 | 本形态没有/未核实 |
|---|---|---|---|---|
| 主编（lead） | seat | （不封任何能力） | — | — |
| 主笔（member） | teammate | ledger.write、tape.write、decision.write、fs.edit、fs.search、shell.exec、image.read | `mcp__novelist__novel_chapter` `mcp__novelist__novel_ledger` `mcp__novelist__novel_init` `mcp__novelist__novel_assemble` `mcp__novelist__novel_outline` `mcp__novelist__novel_event` `mcp__novelist__novel_decide` `mcp__novelist__novel_score` `Glob` `Grep` `Bash` | fs.edit（未核实）；shell.exec（未核实）；image.read（本形态无此工具） |
| 档案员 | subagent | ledger.write、decision.write | `mcp__novelist__novel_chapter` `mcp__novelist__novel_ledger` `mcp__novelist__novel_init` `mcp__novelist__novel_assemble` `mcp__novelist__novel_outline` `mcp__novelist__novel_decide` `mcp__novelist__novel_score` | — |
| 试读员 | subagent | ledger.write、ledger.read、tape.write、ledger.ask、decision.write、retrieval、fs.read、fs.write、fs.edit、fs.search、shell.exec、image.read、spawn、tool.search、tool.invoke、agent.message | `mcp__novelist__novel_chapter` `mcp__novelist__novel_ledger` `mcp__novelist__novel_init` `mcp__novelist__novel_assemble` `mcp__novelist__novel_outline` `mcp__novelist__novel_bible` `mcp__novelist__novel_verify` `mcp__novelist__novel_count` `mcp__novelist__novel_event` `mcp__novelist__novel_ask` `mcp__novelist__novel_decide` `mcp__novelist__novel_score` `mcp__novelist__novel_context` `mcp__novelist__novel_search` `mcp__novelist__novel_guide` `Read` `Glob` `Grep` `Bash` `ToolSearch` `DeferExecuteTool` `SendMessage` | fs.write（未核实）；fs.edit（未核实）；shell.exec（未核实）；image.read（本形态无此工具）；spawn（本形态无此工具） |
| 结构校准员 | subagent | ledger.write、ledger.read、tape.write、ledger.ask、decision.write、retrieval、fs.read、fs.write、fs.edit、fs.search、shell.exec、image.read、spawn、tool.search、tool.invoke、agent.message | `mcp__novelist__novel_chapter` `mcp__novelist__novel_ledger` `mcp__novelist__novel_init` `mcp__novelist__novel_assemble` `mcp__novelist__novel_outline` `mcp__novelist__novel_bible` `mcp__novelist__novel_verify` `mcp__novelist__novel_count` `mcp__novelist__novel_event` `mcp__novelist__novel_ask` `mcp__novelist__novel_decide` `mcp__novelist__novel_score` `mcp__novelist__novel_context` `mcp__novelist__novel_search` `mcp__novelist__novel_guide` `Read` `Glob` `Grep` `Bash` `ToolSearch` `DeferExecuteTool` `SendMessage` | fs.write（未核实）；fs.edit（未核实）；shell.exec（未核实）；image.read（本形态无此工具）；spawn（本形态无此工具） |
| 校对员 | subagent | ledger.write、ledger.read、tape.write、ledger.ask、decision.write、retrieval、fs.write、fs.edit、fs.search、shell.exec、image.read、spawn、tool.search、tool.invoke、agent.message | `mcp__novelist__novel_chapter` `mcp__novelist__novel_ledger` `mcp__novelist__novel_init` `mcp__novelist__novel_assemble` `mcp__novelist__novel_outline` `mcp__novelist__novel_bible` `mcp__novelist__novel_verify` `mcp__novelist__novel_count` `mcp__novelist__novel_event` `mcp__novelist__novel_ask` `mcp__novelist__novel_decide` `mcp__novelist__novel_score` `mcp__novelist__novel_context` `mcp__novelist__novel_search` `mcp__novelist__novel_guide` `Glob` `Grep` `Bash` `ToolSearch` `DeferExecuteTool` `SendMessage` | fs.write（未核实）；fs.edit（未核实）；shell.exec（未核实）；image.read（本形态无此工具）；spawn（本形态无此工具） |
| 拆书员 | subagent | ledger.write、ledger.read、tape.write、ledger.ask、decision.write、retrieval、fs.edit、fs.search、shell.exec、image.read、spawn、tool.search、tool.invoke、agent.message | `mcp__novelist__novel_chapter` `mcp__novelist__novel_ledger` `mcp__novelist__novel_init` `mcp__novelist__novel_assemble` `mcp__novelist__novel_outline` `mcp__novelist__novel_bible` `mcp__novelist__novel_verify` `mcp__novelist__novel_count` `mcp__novelist__novel_event` `mcp__novelist__novel_ask` `mcp__novelist__novel_decide` `mcp__novelist__novel_score` `mcp__novelist__novel_context` `mcp__novelist__novel_search` `mcp__novelist__novel_guide` `Glob` `Grep` `Bash` `ToolSearch` `DeferExecuteTool` `SendMessage` | fs.edit（未核实）；shell.exec（未核实）；image.read（本形态无此工具）；spawn（本形态无此工具） |

「封掉的能力」一列是宿主无关口径（与 DSH 形态同一张表推导）；「工具真名」一列才是可以写进 disallowedTools 的东西。
同一列里既有名字又有"—"，差别不是重不重要，而是**这个形态下有没有这个工具**：没有工具可禁时，那条 deny 是空操作。

### 连带影响（禁一条会顺带影响什么）

- `novel_event（档案员保留的那件）`：**粒度警告（2026-09-20 第三方实测）**：deny 只有**整工具**粒度，没有 op 级语法，宿主侧 disallowedTools 同样整名匹配 ⇒ 保留 novel_event ＝把 op=append（往事件带写）一并给它。原先文档写「保留 novel_event read」是把意图写成了事实，已改。要在硬面上只给读，只能改连接器侧——宿主层做不到（这也与本项目 REC-04 的结论同源：没有可机器验证的身份，就别在宿主层假装能按角色收权）。
- `DeferExecuteTool`：它同时是 Workflow 的执行器（宿主 DEFER_EXECUTE 会分派 WORKFLOW）⇒ deny 它可能**连带废掉 Workflow 面**。故不把 Workflow 与它合成一条能力：合成的后果是「看着只禁了发现，实际连编排一起禁」，而这种连带在名单里看不出来。

## 四、未核实清单（**不许当已实现**）

这些能力在本宿主下**封不封得住还不知道**：名字形态、或"deny 认不认这个写法"未核实（写错＝静默空转：看起来封了，其实一条也没封）。

- fanout（多代理编排脚本）：主编座位确认有（延迟加载）；成员/普通子代理形态是否存在**未测**（WB 回执 §六 列为未测项）
  - 实测候选：`Workflow`

## 五、会被照抄、但在本宿主不存在的名字

从 DSH 名单直抄过来会**静默空转**的名字（本宿主官方内置工具清单里没有它们）：

| 名字 | 为什么会被写进来 | 本宿主真名 |
|---|---|---|
| `read_image` | DSH 侧真有一个独立图像读工具 | 无独立工具（图像并入 `Read`） |
| `pwsh` | DSH 在 Windows 上的 shell 工具叫这个 | `PowerShell`（Windows 专属；另有 `Bash`） |
| `Task` | 别的宿主用 `Task` 派生子代理 | `Agent`（本宿主的 `Task*` 六件是任务清单，不是派生） |

另有一类**同向**的坑：名字真实存在，但在本形态**缺席**（如团队成员形态的 `Agent`/`TeamCreate`）——
禁它们不是"封住了"，是**空操作**（没有工具可禁）。第三节的「本形态没有/未核实」一列就是为它设的。

## 五b、存在、但**本形态下不可用**的名字（不许读成"已封"）

这一类与上一节**后果同类、成因不同**：上一节是名字不存在；这里是名字存在、能发起调用，但会被宿主拒。

- `SendMessage` @ **subagent**：**存在、但本形态下不可用**——名字真实，不在团里的普通子代理发起调用会被宿主拒：`Not in a team. SendMessage is only available when you are in a team or have background agents running.`（2026-09-21 WB 侧「强行发起一次调用」实测，硬返回）
  - 于是怎么办：对以 subagent 形态跑的五个盲角色，封它是**空操作**（没有可用的通道可封）。它与 nonexistent 那三个**后果同类、成因不同**：那三个是名字不存在，这个是名字存在但本形态下惰性。要防它，只能改用 teammate 形态并另算语义（teammate 里 SendMessage 是向主编回报的**正常通道**，封掉＝断回报——同一格能力在两种形态下语义相反）。

## 六、与 DSH 形态的强度差异（读者需要知道的那部分）

| 机制 | DSH 形态 | 本形态（现状） |
|---|---|---|
| 落账权隔离（只有主编能写账本） | 宿主强制：子代理 deny 掉写账本工具 | **机制已实测拦得住**（`disallowedTools` 中性同形探针红测），但**须把定义投放到项目级落点**；本包当前投放数＝**0**，故现状仍等于纯纪律 |
| 盲读输入隔离（盲角色只能读派工包） | 宿主强制：读写文件工具全封 | 同上（同一机制）。五工种定义建好之前＝纯纪律 |
| 盲角色不得发现/执行未加载的工具 | 宿主强制：本预设根本没挂这条通道 | **实测：通道存在且可执行**；但**对盲角色可以封**（`ToolSearch`+`DeferExecuteTool` 一对，封了＝切断全部 MCP）——机制已实测，定义未建 |

引用本形态的盲读结论做重要判断时，请带上这条限定。日常写作不受影响。

## 七、给实测用：只含已核实真名的声明片段（★读之前先看"落点"在不在）

下表这几条**只由已核实的真名组成**（含已实测的 MCP 名），可以直接拿去做运行时红测。

**★ 落点只有一处是真的：项目级自定义 agent 定义 `<工作区>/.codebuddy/agents/<name>.md`。**
三条 2026-09-22 实测（WB 侧 `[硬返回]` ＋ 宿主源码，仓侧已独立读源码复核前两条）：
1. **包内 `agents/` 不是注册载体**——宿主组件装载器**不枚举专家包**（日志里 11 个插件各有 `Loaded plugin components for …`，本包一次都没出现）；包内那两份的真实用途是**人格文本的权威来源**，往里补前言块不会让它进表。
2. **项目级定义当次生效**（实测不需重启；插件内定义才随组件加载、改完要重启——两条路别混）。
3. **deny 真拦得住**：中性同形探针 `disallowedTools:[mcp__novelist__novel_chapter]` → `Error: Permission to use mcp__novelist__novel_chapter has been denied.`（拦在权限层，未到工具本体）。

**★ 红测必须用中性同形探针，不要借本座。**WB 侧第一版让"主笔去调它自己被禁的工具"，它**以纪律为由拒发**——
**人格纪律与机器 deny 是混淆变量**，那样测出来的"拒"分不清是谁干的。
另：**主笔本座至今没有机械读数**（两次自守拒发）⇒ 口径只能写"机制已实测成立（同形配置）"，**不许写成"主笔已被机器封死"**。

**★ 两条省事的取证路径（先走第 1 条，不用重启也不用派探针）**：
1. 日志行 `[AgentTask] agent lookup failed | requested="X" | available=[...]`——那行 `available` 就是现成仪器，直接看"宿主这一刻加载了哪些 agent"。
2. 用户级目录＝app 进程环境变量的 `CODEBUDDY_CONFIG_DIR` 下的 `agents/`（**本机该变量指向宿主自己的配置目录 `.workbuddy`** ⇒ 实为 `~/.workbuddy/agents/`）；
   **以环境变量为准，不要照抄本条的字面值**——换个装法就会分叉。（本节不写机器绝对路径：包内文档零机器路径是硬门。）

**★ 五个工种的落点＝待建**：`references/roles/` 下的件是派工文本、不是 agent 定义（往里加 frontmatter 不会有任何效果）。
要建就建成**项目级定义**（路径同上）；且**建之前先过耦合不变量**——本形态 MCP 只能经 `ToolSearch`→`DeferExecuteTool` 到达，
**封发现面的角色必须同时封掉全部 MCP 面能力**（生成器会在装配期拦下不满足者）。

- **主笔**（agents/author.md，形态 teammate）：`disallowedTools: [mcp__novelist__novel_chapter, mcp__novelist__novel_ledger, mcp__novelist__novel_init, mcp__novelist__novel_assemble, mcp__novelist__novel_outline, mcp__novelist__novel_event, mcp__novelist__novel_decide, mcp__novelist__novel_score, Glob, Grep, Bash]`
  - **⚠ 缺口（已实测拦不住，故不写进名单）**：`PowerShell`（能力 shell.exec）——写进 `disallowedTools` 后**它仍在工具面里、且调用真的会执行**。**不许写进去装作封住了**：静默列进去＝虚假的约束感。本行是**如实标注**，不是"以后会修"。
  - 落点：**包内 `agents/` 不是注册载体**（组件装载器不枚举专家包）。本行是**人格文本的权威来源**；要让它真的生效，需把同一份文本投放成**项目级定义** `<工作区>/.codebuddy/agents/<name>.md`（投放即生效、不用重启；见本节开头）
- **档案员**（references/roles/archivist.md，形态 subagent）：`disallowedTools: [mcp__novelist__novel_chapter, mcp__novelist__novel_ledger, mcp__novelist__novel_init, mcp__novelist__novel_assemble, mcp__novelist__novel_outline, mcp__novelist__novel_decide, mcp__novelist__novel_score]`
  - 落点：**待建**——`references/roles/archivist.md` 是派工文本、不是 agent 定义。要建就建成**项目级定义**（路径同左）；建前先过耦合不变量：封发现面者必须封掉全部 MCP 面能力
- **试读员**（references/roles/reader.md，形态 subagent）：`disallowedTools: [mcp__novelist__novel_chapter, mcp__novelist__novel_ledger, mcp__novelist__novel_init, mcp__novelist__novel_assemble, mcp__novelist__novel_outline, mcp__novelist__novel_bible, mcp__novelist__novel_verify, mcp__novelist__novel_count, mcp__novelist__novel_event, mcp__novelist__novel_ask, mcp__novelist__novel_decide, mcp__novelist__novel_score, mcp__novelist__novel_context, mcp__novelist__novel_search, mcp__novelist__novel_guide, Read, Glob, Grep, Bash, ToolSearch, DeferExecuteTool, SendMessage]`
  - **⚠ 缺口（已实测拦不住，故不写进名单）**：`PowerShell`（能力 shell.exec）——写进 `disallowedTools` 后**它仍在工具面里、且调用真的会执行**。**不许写进去装作封住了**：静默列进去＝虚假的约束感。本行是**如实标注**，不是"以后会修"。
  - 落点：**待建**——`references/roles/reader.md` 是派工文本、不是 agent 定义。要建就建成**项目级定义**（路径同左）；建前先过耦合不变量：封发现面者必须封掉全部 MCP 面能力
- **结构校准员**（references/roles/calibrator.md，形态 subagent）：`disallowedTools: [mcp__novelist__novel_chapter, mcp__novelist__novel_ledger, mcp__novelist__novel_init, mcp__novelist__novel_assemble, mcp__novelist__novel_outline, mcp__novelist__novel_bible, mcp__novelist__novel_verify, mcp__novelist__novel_count, mcp__novelist__novel_event, mcp__novelist__novel_ask, mcp__novelist__novel_decide, mcp__novelist__novel_score, mcp__novelist__novel_context, mcp__novelist__novel_search, mcp__novelist__novel_guide, Read, Glob, Grep, Bash, ToolSearch, DeferExecuteTool, SendMessage]`
  - **⚠ 缺口（已实测拦不住，故不写进名单）**：`PowerShell`（能力 shell.exec）——写进 `disallowedTools` 后**它仍在工具面里、且调用真的会执行**。**不许写进去装作封住了**：静默列进去＝虚假的约束感。本行是**如实标注**，不是"以后会修"。
  - 落点：**待建**——`references/roles/calibrator.md` 是派工文本、不是 agent 定义。要建就建成**项目级定义**（路径同左）；建前先过耦合不变量：封发现面者必须封掉全部 MCP 面能力
- **校对员**（references/roles/proofer.md，形态 subagent）：`disallowedTools: [mcp__novelist__novel_chapter, mcp__novelist__novel_ledger, mcp__novelist__novel_init, mcp__novelist__novel_assemble, mcp__novelist__novel_outline, mcp__novelist__novel_bible, mcp__novelist__novel_verify, mcp__novelist__novel_count, mcp__novelist__novel_event, mcp__novelist__novel_ask, mcp__novelist__novel_decide, mcp__novelist__novel_score, mcp__novelist__novel_context, mcp__novelist__novel_search, mcp__novelist__novel_guide, Glob, Grep, Bash, ToolSearch, DeferExecuteTool, SendMessage]`
  - **⚠ 缺口（已实测拦不住，故不写进名单）**：`PowerShell`（能力 shell.exec）——写进 `disallowedTools` 后**它仍在工具面里、且调用真的会执行**。**不许写进去装作封住了**：静默列进去＝虚假的约束感。本行是**如实标注**，不是"以后会修"。
  - 落点：**待建**——`references/roles/proofer.md` 是派工文本、不是 agent 定义。要建就建成**项目级定义**（路径同左）；建前先过耦合不变量：封发现面者必须封掉全部 MCP 面能力
- **拆书员**（references/roles/dissector.md，形态 subagent）：`disallowedTools: [mcp__novelist__novel_chapter, mcp__novelist__novel_ledger, mcp__novelist__novel_init, mcp__novelist__novel_assemble, mcp__novelist__novel_outline, mcp__novelist__novel_bible, mcp__novelist__novel_verify, mcp__novelist__novel_count, mcp__novelist__novel_event, mcp__novelist__novel_ask, mcp__novelist__novel_decide, mcp__novelist__novel_score, mcp__novelist__novel_context, mcp__novelist__novel_search, mcp__novelist__novel_guide, Glob, Grep, Bash, ToolSearch, DeferExecuteTool, SendMessage]`
  - **⚠ 缺口（已实测拦不住，故不写进名单）**：`PowerShell`（能力 shell.exec）——写进 `disallowedTools` 后**它仍在工具面里、且调用真的会执行**。**不许写进去装作封住了**：静默列进去＝虚假的约束感。本行是**如实标注**，不是"以后会修"。
  - 落点：**待建**——`references/roles/dissector.md` 是派工文本、不是 agent 定义。要建就建成**项目级定义**（路径同左）；建前先过耦合不变量：封发现面者必须封掉全部 MCP 面能力

预期：加上之后该 agent 调这些工具应当直接失败或被拒。**若照样能调通，说明本形态的 deny 不生效**——那就要如实降级到"纪律约束"，并把这条写进交付说明，而不是当它生效了。

两条本宿主实测纪律：①agent 定义**不热加载**，改完必须重启，验证天然跨会话；②"某工具缺席"类结论拿不到硬证据（不在清单里就发不起调用）——唯一出口是**强行发起一次对它的调用**，逼调度层回 `Tool Not Found`。

---

## 附：怎么自己核这份文件

1. 本宿主官方工具清单里应能找到第三节引号内的全部名字（「本形态没有/未核实」列出的那批除外）。
2. 第五节的名字在本宿主清单里**应当找不到**——找到了说明我搞错了宿主，请指出来。
3. 第三节的名单与「封掉的能力」一列应由同一张能力表推导；若你看到两边对不上，是生成器的 bug。
