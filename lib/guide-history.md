# novelist-guide 机制沿革（v6 → v7 逐版增量）

> 从 `lib/novelist.js` 的常驻 guide 移出（2026-09-17，提示词瘦身批）：常驻只留当前生效口径，
> 版本沿革在此按需查阅。**本文件不覆盖当前行为**——当前口径以 `lib/novelist.js` 的 SECTION 为准；
> 本文件回答的是"某条机制是哪一版加的/哪版改的口径/哪版作废了什么"。

novelist-guide v7.4 · 机制细则唯一真源（novelist 插件同版注入；preset persona 只含决策原则与指针）。书工程 = book_dir（一部书一个目录；novel_* 工具一律显式传 book_dir 绝对路径）。身份与协作框架见 persona 段。v6 增量（2026-09-04）：冷读审法换三问量表、前情事实卡、盲角色派工纪律四条；v6.1：编辑部五角色→六角色（+结构校准员，one-shot 纯盲只见正文）；v6.2：P0 回改定稿前 A/B 冷读门、商业工位（书名简介/责编评分卡）；v6.3（修订轮）：校对员 one-shot+交付预算纪律（preset 侧）、编排熔断、写手落盘无题首行；v6.4（W-1 双臂实测后）：角色运输二分——策划/档案员=常驻同事（continuable 起+s2s session_id 唤醒），写手/试读/校准/校对=一次性前台 spawn（盲读与写入口安全不进同事线）；v6.5（V3 P1，2026-09-05）：事件带 novel_event＋仪器纪律预告＋锚参字段与 selection_report gate＋版本戳三处对齐；v6.6（ADR-034，2026-09-13）：戏剧工位+结构主方法换向+choice_axis+B9 恢复；v6.7（ADR-034 拆件撤+总蓝图第一批，2026-09-13 夜）：戏剧工位撤编制改走向呈报制（判平证伪：主编任结构作者=盲人领路；choice_axis 字段保留）；章级细纲三行制（拍点/钩子/个体参照——关键章全看+常规三行+每3抽1+触发升格连看3章）；章长窗默认 3000-4000、日节奏 6 章/批；结构方法改策略池制（单源移植=v0，锚池扩语料库全集）；判据降权（量表/z-score=守门工具，"写得好"不得引为证据——证据唯 Owner 裁决记录+语料库个体范本）；**v7.0（ADR-035·R1 三原语，2026-09-16）：novel_ask 问账本原语（语义路由+出处组装，零 LLM，一次替代 bible 过滤+event read+verify 翻账）＋novel_chapter 派生模式（decision 随章落带/advance_to 状态推进/editorial/txn 完成回执）＋每章执行流改三原语（主编手工工具调用目标 ≤3 次/章）**；**v7.1（提示词梳理批，2026-09-16 夜）：①状态机迁移入口口径修正——三入口同一张迁移表（novel_chapter advance_to / novel_decide status 派生 / novel_ledger set_chapter_status），"唯一入口"旧口径作废；②主编 persona 重铸为三段式（身份/场域/纪律）＋裁量条款——guide 流程=缺省轨道，主编可偏离任何非红线流程、novel_event 留痕即可，红线（账本写入口/盲读隔离/ask 门/状态机/证据引用/判据降权）不可偏离；③角色分工 home 收归本目录"派工分工"行（persona 不再复述，单一真源防口径分叉）**；**v7.2（ADR-036 两座位制，2026-09-16 夜）：①编制瘦身 8→2——常设=主编+主笔（原策划+写手合并：作者思维一体、署名提案+亲自执笔、章内两段式=提案→Owner 门→同会话续写、continuable 同事线）；②按需工种（一次性 spawn 不占编制）=档案员（R1 后账本自动派生降格按需）/拆书员/试读员/校准员/校对员（过渡期盲读工种，R2 批处理器上线后收编为并行采样：角色撤销、测量保留）；③文中旧称"写手"皆指主笔的执笔职能；④撤编不删教训：连续性由文件承载/上下文不成长红线/交付落盘+工具口径核数，全部随执笔职能移交主笔**；**v7.3（毒舌评审修正批，2026-09-16 夜，lab/audit-0916 三实例同坐标聚类）：①派工配方四层→五层——新增"上章反馈卡"（Owner 裁决摘要+审稿分歧点入派工包，修复生成端零反馈回路）；②批末金丝雀冷读条款（每批 1 章随机 A-B 盲读=平信号哨兵，对冲"生产环路无组件能报平"与细纲呈报友好化作弊通道）**；**v7.4（R2 施工批，2026-09-16 深夜）：①novel_decide 撤销语义（supersedes 引用即作废，append-only 不改历史，readTapeWindow 出 superseded_ids 供聚合过滤；重复撤销拦）②批审协议落地（并行盲采样 N 阶梯 2→6+batch-aggregate 代码聚合——引文定位/段落聚类/k=2 投票/单报盲存/伪引文拒收/≤10 条逐条采纳式；k/N/权重初值公示可调，验收挂生产数据）③审稿工种进入批审模式：主编一键 spawn 采样员→聚合→只看分歧点**；**v7.5（批A1 事务可信，2026-09-17）：①novel_chapter 回执绑定正文（content_hash=sha256 正文＋status pending→done；中断=残留 pending，账务未收束）②同参重试幂等——正文逐字相同即判为重试/续跑，不翻版本快照、不重复记账，返回 deduped:true（断点续跑可直接重交，安全）③expected_rev 并发护栏（声明"我读到的账上 rev"，不符=写入前拒绝且零副作用；新章报 0）④novel_verify 回执对账——pending 回执报半提交、done 回执 content_hash 与正文不符报"正文被绕过工具改动"（旧回执无 hash 字段=legacy，不误报）。**断点续跑判定口径=读 editorial/txn/ 的 status（done 才算这章真写完），不是"正文文件在不在"**；**v7.6（批A2 事实版本化，2026-09-17）：①事实贡献账 editorial/facts/chapter_XXX.vN.json——每章每版记"本版贡献了哪些事实"（seeds/closes/timeline/cast），append-only：共享账是聚合态，改稿会覆盖掉旧版贡献，回退与版本化裁决就没有依据②last_seen 按有效出场重建——改稿删掉某角色的出场时，其 last_seen 回退到最近有效章（旧实现只在提交时单调 max，删不掉人）；纯旧书无贡献账的人物一律不动③判据账版本绑定——每条判词自动绑 ch_rev+content_hash，novel_score read 带 ch 时默认只回当前正文版本的判词（改稿后旧判词不再冒充当前版结论，include_stale=true 可连同旧版取并逐条标 stale/legacy）。派工包里的前情事实卡一律取当前有效版事实**；**v7.7（批E2 novel_ask 有效投影内核，2026-09-17）：①timeline 纳入读取（旧实现完全没读 timeline.json——问"时间线/发生了什么"答不出来）②supersedes 有效投影落到消费端：被作废条目不进事件带窗口正文、不进未闭欠线（撤销链 superseded_chain 单独可查，作废这个事实不静默丢失）③ch 参数=基准章统一投影：时间线只回 ≤ch、人物 last_seen 按有效事实投影到 ≤ch（超前写好的存稿不算"当前账"——旧实现把 ch20 的出场答进 ch10 的问）④select 结构化选择器与自然语言 q 并存：{entity 实体名｜kind=characters/bible/foreshadows/timeline/status/tape｜window:{from,to}}，确定性取数不靠分词**。**v7.8（A+B 检索批，2026-09-18）：①**novel_search 正文检索**（第 14 个工具）——manuscript/ 当前正文的字面查找，返回 章号＋行号＋命中片段；多词=AND；ch/window 限章；limit 截断明报；不做语义近似/摘要/读快照。**定位=候选召回不是事实源**；**主笔直调**（Owner 拍板：主笔要能自查旧文——宿主权限按工具名不按路径，主笔被 deny glob/grep＝连书目录都搜不了，放开＝放开整块硬盘；故"只能搜书目录"的门只能在插件里开）；盲角色四工种连同 novel_context 一并 deny。②**novel_context op=factsheet 前情事实卡投影**（缩形版）——六节确定性事实（应兑现伏笔含逾期标记/既有钩子含未闭欠线/近场人物/世界观规则/时间线锚/禁改清单），ch 缺省=current_ch+1、near 缺省 8，每条带章号坐标；**叙述性一节（上章末状态）不代写**，返回里明标由主编补写。③工具数 13→14（registry/versions/preset deny 三处断言同步）**。**〔v7.8 明确不做（Owner 拍板）：向量/嵌入/图检索路线暂不采用——实测 50 万字正文≈30 万 token，模型窗口 1e6，容量不是瓶颈；一手证据：Mem0 消融显示图结构相对向量基线仅 +2%，Anthropic 工程指引主张"即时检索/渐进披露"而非预加载；真缺口＝正文层缺受控入口，已由 novel_search 补上。语义检索留待"关键词召回实测不达标"证据齐备后再议，代价是引入外部依赖〕**。
